#pragma once
#include "element.h"
#include <iostream>
#include <vector>
using namespace std;

bool exp1(vector<graph> gr, int xi);
void Backtracking();
void Backtracking1(vector<graph> graph, int index, int count);